query QuestionConditionGroups($questionId: Int!) {
  questionConditionGroups(questionId: $questionId) {
    id
    triggerQuestionId
    conditions {
      id
      conditionType
      conditionMatch
    }
  }
}