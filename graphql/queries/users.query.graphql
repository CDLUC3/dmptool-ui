query Me{
  me {
    id
    givenName
    surName
    languageId
    role
    emails {
      id
      email
      isPrimary
      isConfirmed
    }
    errors {
      general
      email
      password
      role
    }
    affiliation {
      id
      name
      displayName
      searchName
      uri
      acronyms
      feedbackEmails
      feedbackEnabled
      feedbackMessage
    }
  }
}

query Users($paginationOptions: PaginationOptions, $term: String) {
  users(paginationOptions: $paginationOptions, term: $term) {
    items {
      givenName
      surName
      email
      active
      role
      created
      last_sign_in
      plans {
        id
        title
        dmpId
      }
      id
    }
    hasNextPage
    currentOffset
    hasPreviousPage
    totalCount
    nextCursor
    limit
  }
}

